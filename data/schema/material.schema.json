{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TMCC material record",
  "type": "object",
  "required": [
    "material_id",
    "slug",
    "family",
    "material_type",
    "subclass",
    "structure_type",
    "formula",
    "host",
    "intercalation",
    "experimental_status",
    "calculation_status",
    "structure",
    "thermodynamics",
    "phonons",
    "mechanical",
    "electronic",
    "energy_storage",
    "files",
    "provenance"
  ],
  "properties": {
    "material_id": { "type": "string", "pattern": "^TMCC-\\d{4,}$" },
    "slug": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
    "family": { "const": "TMCC" },
    "material_type": { "enum": ["pristine", "tm_intercalated", "m2xa"] },
    "subclass": { "enum": ["TMCC", "TMCDC"] },
    "structure_type": { "enum": ["M2XC", "M2XN", "M2X2C", "M2X2N", "M2X2A", "M2XA"] },
    "formula": { "type": "string" },
    "host": {
      "type": "object",
      "required": ["formula", "metal", "chalcogen", "anion"],
      "additionalProperties": false,
      "properties": {
        "formula": { "type": "string" },
        "metal": { "type": "string" },
        "chalcogen": { "enum": ["S", "Se", "Te"] },
        "anion": { "enum": ["C", "N", "P", "As", "Sb", "Bi", "Si", "Ge", "Sn", "Pb", "B", "Al", "Ga", "In"] }
      }
    },
    "intercalation": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "required": ["intercalant", "x", "mode", "site", "ordering", "configuration"],
          "properties": {
            "intercalant": { "type": "string" },
            "x": { "type": "number" },
            "mode": { "enum": ["self", "hetero"] },
            "site": { "type": ["string", "null"] },
            "ordering": { "type": ["string", "null"] },
            "configuration": { "type": "string" }
          }
        }
      ]
    },
    "experimental_status": { "enum": [null, "unknown", "experimental", "not_reported", "computational"] },
    "calculation_status": { "enum": ["not_calculated", "calculation_in_progress", "calculated"] },
    "structure": {
      "type": "object",
      "properties": {
        "cell_volume": { "$ref": "#/$defs/unitValue" },
        "density": { "$ref": "#/$defs/unitValue" }
      },
      "additionalProperties": true
    },
    "thermodynamics": {
      "type": "object",
      "properties": {
        "formation_energy": { "$ref": "#/$defs/unitValue" },
        "formation_energy_per_atom": { "$ref": "#/$defs/unitValue" }
      },
      "additionalProperties": true
    },
    "mechanical": {
      "type": "object",
      "properties": {
        "mechanically_stable": { "type": ["boolean", "null"] },
        "elastic_constants": { "type": ["object", "null"] }
      },
      "additionalProperties": true
    },
    "files": { "type": "object" },
    "provenance": { "type": "object" }
  },
  "$defs": {
    "unitValue": {
      "type": "object",
      "required": ["value", "unit"],
      "properties": {
        "value": { "type": ["number", "null"] },
        "unit": { "type": "string" }
      },
      "additionalProperties": false
    }
  }
}
